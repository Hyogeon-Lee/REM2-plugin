# md-convert 의존성
#
# 핵심 (이미지 in-place 매칭을 위한 직접 파싱)
python-docx>=1.0.0    # DOCX
python-pptx>=0.6.21   # PPTX
openpyxl>=3.1.0       # XLSX
PyMuPDF>=1.23.0       # PDF (이미지+텍스트 좌표 정렬)
lxml>=4.9.0           # XML 파싱 (DOCX/PPTX/HWPX 공통)
olefile>=0.46         # HWP 5.x OLE2 컨테이너

# 이미지 PNG 통일 변환
Pillow>=10.0.0        # 래스터 재인코딩 + 다운샘플
# Windows GDI (WMF/EMF 래스터화)는 ctypes (stdlib)로 호출 — 추가 패키지 불필요

# 선택: 기타 포맷 fallback (CSV/HTML/오디오/EPUB 등)
markitdown[all]>=0.1.0
